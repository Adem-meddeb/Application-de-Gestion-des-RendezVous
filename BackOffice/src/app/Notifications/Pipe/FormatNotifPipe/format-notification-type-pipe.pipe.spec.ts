import { FormatNotificationTypePipePipe } from './format-notification-type-pipe.pipe';

describe('FormatNotificationTypePipePipe', () => {
  it('create an instance', () => {
    const pipe = new FormatNotificationTypePipePipe();
    expect(pipe).toBeTruthy();
  });
});
